# 提示词：用 AI 做一个「序列压力测算」工具

把下面**「提示词正文」**整段复制给 AI（Claude / GPT / 任意），它就能从零做出一个可用版本。

这不是"照着这个仓库抄"，而是把**领域模型**和**我们踩过的坑**一次讲清楚——那些坑不写进去，AI 几乎一定会踩，你自己也不一定看得出来（有几个是画出来才发现的）。

想要更好就在末尾加你的要求（换领域、加维度、改交互），前面这些是地基。

---

## 提示词正文

> 帮我做一个**单文件交互网页工具**：把一条「人群序列」摆成位次轴，测算某个岗位区间里每一层承受的**挤压**和**替代**压力。
>
> 打开 HTML 就能用——**不要构建步骤、不要 npm、不要 CDN**。

### 一、领域模型

**序列** = 若干「层」，每层 `{名称, 人数, 位次}`。位次是整数，**可以并列**（多层同位次）。

**岗位** = `{名称, 类型, 下限层, 上限层}`。类型两种：

**普通岗**——按位次分上下：

- **挤压** = 位次**同级或更高**的所有层的人数之和，**含本层自己**
- **替代** = 位次更低的所有层的人数之和
- 为什么同层算压力：这些人跟你学历等价，投的是同一批岗，是**直接竞争对手**，不能排除
- 自己那一个人也一并算进去，**不要单独减 1**——减了满屏都是 `999` 结尾

**考试岗**——只看分数和门槛，过了线学历高低不再区分谁强谁弱：

- **挤压 = 替代 = 区间内总人数（含本层）**
- 结果与"是哪一层"无关 → 曲线是一条**水平线**
- 直觉上会以为考试岗压力应该按"除我以外的人"算，**那是错的**：它描述的是**池子规模**，你自己也在池子里

**两条修正**（都要能在界面上调）：

- **边界渗透 `k`**（默认 0.15）：岗位区间不是硬墙。区间外**紧邻**的一档按 `k` 计入，再往外按距离次幂衰减。理由是"总有人跨区间乱投"
- **基准竞争 `base`**（默认 500）：每层垫一个固定底

**公式**（普通岗，层 `l`）：

```
挤压(l) = base + Σ  w(tier(ol)) · count(ol)     对 tier(ol) ≥ tier(l)
替代(l) = base + Σ  w(tier(ol)) · count(ol)     对 tier(ol) <  tier(l)

w(t) = 1            若 lo ≤ t ≤ hi
     = k^(lo − t)   若 t < lo
     = k^(t − hi)   若 t > hi
```

### 二、界面

**左边：序列编辑器**

- 拖拽换位次；拖进同一行 = 并列
- **拖到行与行之间的缝 = 抽出来单独成一行**
- ⚠️ **合并和拆分必须都能做**。只能合并不能拆是这类编辑器的常见缺陷——用户会把两层拖到一起，然后发现再也分不开，只能删了重建
- 每层填人数
- 标题里放一个**可编辑的口径标签**，默认「学历」。改成「专业」，整套就按专业看人数——工具本身不该关心这一列装的是什么，它只认「有人群 + 人数 + 位次」

**右边：岗位编辑器 + 曲线图**

- **颜色编码「岗位」，线型编码「压力类型」**（实线 = 挤压，虚线 = 替代）
- ⚠️ **不要反过来**用颜色编码类型。反过来的话两个岗位就只有两种颜色、四根线挤在一起完全分不开，这是个非常容易犯的错
- **考试岗挤压 = 替代，只画一根线**。画两遍会让后画的虚线盖住前面的实线，看上去是条虚线，图例却写着"实线 = 挤压"

**人数金字塔**（放在序列编辑器下面）

- 按位次**自下而上**画各层人数：位次最低的（人最多）在最底最宽，往上逐层收窄——低学历人多、高学历人少，这个形状本身就是真实结构
- 把每个岗位的区间铺成一条**淡色带**，带的上、下两端各画一条实线，标明该岗位的**上/下线**落在哪两层之间
- 松手后两带的重叠区就是被两个区间同时覆盖的学历
- 注意：形状**不保证单调收窄**（数据里可能有凸起，比如硕士段人特别多）。别在说明文字里承诺它是完美三角

### 三、必须避开的坑

1. **曲线不要用 Catmull-Rom 插值。**
   数据里到处是"上升后走平"（比如顶端几层取值相同），Catmull-Rom 在这种拐角会**过冲**，把曲线甩到 0 以下画出负值。
   用 **Fritsch–Carlson 单调三次插值**：保证段内不越界，同时仍然精确穿过每个数据点。

2. **纵轴不要从 0 起。**
   数据可能跨 200 倍，线性轴上最小值只占全高 0.5%，**必然贴死底边**。
   用**对数轴**（刻度取 1-2-5 整数字面），并且——
   **下界要低于最小值一整档刻度**。只比最小值低一点是没用的：最低那条网格线还会落在最小值上，曲线照样压着底线。
   （实测：线性轴下想让最小值离底边 41px，轴的起点得是**负数**。所以这是刻度之间的取舍，不是没调好。）

3. **颜色编码压力类型**——见上，别这么干。

4. **考试岗画两根线**——见上，只画一根。

5. **拖拽只能合并**——见上，必须双向。

6. **同层不算压力**——见上，同层是直接竞争对手。

### 四、验证要求

- 把计算逻辑抽成**纯函数模块**，配单元测试。覆盖：普通岗上下不对称、并列层取值相同、考试岗水平线、渗透按距离衰减、基准兜底、区间端点不在序列里返回空
- 再加一个**用最小 DOM 桩把页面脚本真跑一遍**的自检脚本——只做语法检查抓不到运行期错误（比如某个元素没建出来）
- 测试断言**不要写字面量人数**，按名字从默认数据里取。否则改一次数据就要满文件改断言
- 别再写一个"只看语法"的检查就交差——真正会炸的是运行期

### 五、交付

- `index.html` 打开就能用；引擎可以是独立 js，但页面本身**零依赖**
- 引擎 **UMD** 化，浏览器和 Node 都能 `require`
- **数字全部标明是示意数据**，在 README 里写清楚——不要让人误以为是真的统计

---

## 附：这些坑是怎么来的

留给想深挖的人。每条都是花了几轮才弄对的，不是凭空列的清单。

| 坑 | 怎么发现的 |
| --- | --- |
| Catmull-Rom 过冲 | **上线前算出来的**：顶端两层取值相同那段，算出的控制点 y 超出了画布下沿，会把曲线画到 0 以下 |
| 纵轴贴底 | 改完线性截断轴还是贴底。**量了一下才发现**：我一直在拿"数据点到画布底边"的距离说事，但图画到那儿就没了，那里根本没有线——眼睛看到的"底边线"是最低那条**网格线**，而它当时正好落在最小值上 |
| 对数轴下界 | 第一次取下界 = 最小值 ÷ 2，250 到 500 之间没有任何刻度，最低刻度还是落在 500 上。**必须低一整档** |
| 考试岗画了两遍 | 用户指出"理论上是一条直线"，查代码才发现两个 `drawCurve` 都调了，虚线把实线盖住了 |
| 考试岗口径算错 | 第一版按"除本层以外的人"算，得出一条弯曲的线。用户纠正：应该是**池子规模**，与是哪一层无关。**模型口径错了，数字再精确也没用** |
| 同层不算压力 | 用户的观察：同层的人学历等价、投同一批岗，是直接竞争对手。原来的判据是"同 tier 一律跳过"，改成了按位次比较 |
| 拖拽只能合并 | 用户报的："可以合并，但是拖不出来了"。查代码——只有 `moveLayerTo`，**根本没有拆出来的路径** |
| 自己也要算进去 | 问"算不算自己"，答："自己就一个人，非要减去 1 吗，结尾全是 999" |

**一句话总结**：这个工具的难点不在画图，在**口径**。先把"压力到底是谁对谁"定义清楚，再动手画——定义错了，曲线画得再漂亮也是错的。
